# Native-Memory Milestones (feat/native-memory-reactive)

Worktree: C:\Projects\_worktrees\pluresdb-native-memory (off main 0ec9523)\nDirective: Paradox - improve native bindings to deliver features; Effort 1 & 2; complete not quick.

## Locked design decisions (grounded in source, 2026-07-02)
- **NAPI crate:** crates/pluresdb-node (napi 3.9, features napi6+serde-json).
- **Headroom (E1):** ALREADY built+#[napi]-exported (compressText/countTokens/detectContentType, lib.rs L1369-1439) + 31 tests + e2e proof. Plugin consumes STALE published 2.0.0-alpha.1 => work is DELIVERY (build current pkg -> plugin consume -> MemoryFlushPlan resolver), not dev.
- **Reactive subscribe (E2) design:** notification SOURCE already wired (every write publishes SyncEvent::NodeUpsert on SyncBroadcaster). Gap = subscribe() drops the receiver. FIX = std::thread + broadcast::Receiver<SyncEvent>.blocking_recv() draining into a napi ThreadsafeFunction callback delivering {kind,id} to JS. tokio ALREADY a workspace dep (pluresdb-sync uses broadcast) => add `tokio.workspace=true` to pluresdb-node, NO napi/tokio_rt runtime needed (avoids runtime nesting). Add subscribe(cb)->handle + unsubscribe; clean thread shutdown on drop. NO polling, NO dropped-event stub.
- **Reactive .px (E3):** wire execIr/pxLoadPxSource procedure exec onto the E2 subscription path; .px-first policy.
- **Plugin integration seam (E1/S4):** pluresdb.ts loads addon via createRequire('@plures/pluresdb-native'); free fns available on the module. index.ts register() adds api.registerMemoryFlushPlan(...) beside registerMemoryCapability; add reserveTokensFloor config; fix the stale "no flush-plan resolver" comment.

## Stage status
- [x] S0 baseline build (cargo build -p pluresdb-node --release) - **PASS 2026-07-02 07:40**: clean release build (green) + **31/31 headroom tests pass** (real cl100k token counts, byte-boundary/unicode safe, log-run-collapse accurate). Baseline solid, headroom native code is production-quality (no stubs). Note: `cargo build` emits .rlib/.d only; the consumable `.node` addon is produced by the napi/npm build in S1.
- [ ] S1 Effort-1 headroom delivery + MemoryFlushPlan resolver - verify compression on real high-token context.
- [x] S2 Effort-2 real async subscribe(cb)+unsubscribe - **DONE & VERIFIED 2026-07-02 11:47.** std::thread + `broadcast::Receiver<SyncEvent>::blocking_recv()` -> napi `ThreadsafeFunction` (CalleeHandled=false), `{kind,id}` payload; `unsubscribe` via AtomicBool cancel flag; registry `HashMap<u32,Arc<AtomicBool>>`+`AtomicU32`. No extra Tokio runtime. cargo check green; release .node rebuilt (34.8MB). Node test PASS (exit 0): put triggers callback WITHOUT polling; 2nd put delivered; unsubscribe stops all further callbacks; idempotent unsubscribe safe; clean exit (no leak/hang).
- [x] S3 Effort-3 reactive .px on write - **DONE & VERIFIED 2026-07-02 12:47.** Added `subscribePx(cb)->u32` on PluresDatabase: a dedicated std::thread (SAME arch as S2 subscribe: `broadcast::Receiver::blocking_recv`, NO 2nd Tokio runtime) drains the SyncBroadcaster and, per write event, builds a real `AgentContext` from the node just written (action_type=event kind, target=node id, metadata=folded upserted-node `data` object fields via `context_for_event`), projects the constraint read-model, and runs `px_procedures::evaluate` for REAL. Delivers `PxEventJs { kind, id, violationCount, violationsJson }` ONLY when >=1 constraint is violated (clean write => no callback). Reuses the SAME subscription registry so existing `unsubscribe(id)` stops it (one cancel mechanism). **Honesty (C-NOSTUB-001):** POST-write / observe-and-report BY DESIGN - the broadcast fires AFTER put/delete persists, so it CANNOT block/rollback; it computes+delivers real violations, never hardcodes "none". Pre-write blocking stays `pxOnAction`'s job (not duplicated/faked). Doc-comment documents why it's non-blocking. Release `.node` rebuilt `napi build --platform --release` EXIT=0 (41.66s, 36MB, `subscribePx(cb):number`+`PxEventJs` in index.d.ts). **Node smoke test PASS (exit 0)** at crates/pluresdb-node/test-s3-reactive-px.mjs: persisted `metadata.amount <= 100` constraint fires reactively on put(amount=500) delivering real `C-AMOUNT-CAP` violation with NO polling; put(amount=50) silent; after unsubscribe put(amount=999) silent. S2 subscribe/unsubscribe preserved; 31/31 headroom tests still green. Commit `8290344`.
- [x] S4 integrate: bump plugin to reactive native build, restart gateway, verify live, close loop. **NATIVE INTEGRATION DONE 2026-07-02 ~13:15.** Fast-forwarded canonical `main` 0ec9523->83c8469 (SAME repo as worktree; clean FF, no divergence) so S2+S3 reactive source (subscribe/unsubscribe/subscribePx + PxEventJs/SyncEventJs) is on the crate the plugin links via `file:../pluresdb/crates/pluresdb-node`. Rebuilt canonical `.node` `napi build --platform --release` EXIT=0 (1m37s, fresh 36.5MB @13:14, generated index.d.ts has subscribe/subscribePx/unsubscribe/PxEventJs/SyncEventJs, REACTIVE_DTS_HITS=11). **GATES (against a NON-DESTRUCTIVE temp copy of the real migrated-store):** (a) S4_NATIVE_GATE PASS - fresh .node exposes PluresDatabase+compressText+countTokens and proto.subscribe/subscribePx/unsubscribe all functions; (b) S4_PLUGIN_RESOLVE_GATE PASS - plugin `require.resolve('@plures/pluresdb-native')` succeeds via pnpm virtual store (`.pnpm/@plures+pluresdb-native@fil_...`); that store copy is a hard-copy WITHOUT the .node sibling, so the plugin loader's `ensureNativeLibraryPath` fallback (nativeCrateRoots walk) correctly points NAPI_RS_NATIVE_LIBRARY_PATH at the canonical fresh .node => reactive present; (c) S4_DIST_E2E_GATE PASS - importing the BUILT dist `pluresdb.js`, constructing PluresLmStore on the temp store copy, running store.recall (native loads via dist's own loader; env auto-set to canonical .node), reactive subscribe/subscribePx/unsubscribe present, subscribePx smoke returns numeric id + clean unsubscribe. headroomAvailable()=true. **NO pnpm symlink repair needed** (loader fallback handles the pnpm no-.node quirk by design). Remaining: gateway restart to load fresh .node live + close epic #1/#6. NOTE: plugin store wrapper does NOT yet actively CALL subscribePx (consolidation stays pull/tick; S3 subscribePx is honest POST-write observe/report) - native reactive is available+reachable, active push-wiring in the plugin would be a separate follow-on, not part of E1-E3 delivery.

## HELD (explicit Paradox gate - do NOT do without ok)
- npm publish of @plures/pluresdb-native (org-wide side-effect) - stage, then ask.
- Push of plugin redact fix babc70f - still held.
